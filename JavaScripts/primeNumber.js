const readline = require('readline');
const primeNumber = readline.createInterface({
input: process.stdin,
output: process.stdout

for (let index = 0;  index < primeNumber;  index++){
	if (primeNumber != 1){
	for(let number = 0; number <= index ; number++ ){
		if (primeNumber % index = 0 && primeNumber / number )
	console.log(primeNumber)
	}	
	});
	
	
	let totalNumber = 150;
	
	
	
	
	
	
	
	
	
	
	
	
	