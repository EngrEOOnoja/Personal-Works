const readline = require('readline');
const primeNumber = readline.createInterface({
input: process.stdin,
output: process.stdout
});
let index = 0;
for (index = 0;  index < primeNumber;  index++)
	if (primeNumber / 1 != 0 && primeNumber / primeNumber != 0 && primeNumber != 1){
	console.log("Number is Prime" + prieNumber);}